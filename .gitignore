*.rs.bk
*.rlib
*.prof*
*.swp
.DS_Store
*.log
target/*

!.cargo/*
.cargo/credentials.toml

extension.wasm
crates/**/target/

# Read-only reference tree (upstream md-lsp), not part of the workspace; kept
# untracked so cargo's VCS guards (e.g. cargo fix) see a clean tree.
vendors/
